// Shared test fixtures for contract test suites (Stories 1.2–1.6)
//
// Each contract story will add its own fixture factory here, e.g.:
//   deployWhitelistRegistry()  — Story 1.2
//   deployOracleController()   — Story 1.3
//   deployLeagueFactory()      — Story 1.4
//   deployLeague()             — Story 1.5
//
// Using loadFixture() from @nomicfoundation/hardhat-network-helpers
// for efficient test isolation (snapshot + revert pattern).

export {};
