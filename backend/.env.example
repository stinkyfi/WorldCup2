# Copy to .env (gitignored). Used by Prisma CLI and the API server.

# PostgreSQL — matches docker-compose.yml at repo root
DATABASE_URL="postgresql://worldcup2:worldcup2_dev@localhost:5432/worldcup2?schema=public"

# HTTP API (Story 1.8)
API_PORT=3001
